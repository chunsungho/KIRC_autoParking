#파이썬에서 외부 txt 파일에 값을 적을 때 쓰는 코드
f = open("test.txt", 'w') #txt 파일 열기 'w'는 쓰기 전용으로 열었다는 
#for x in range(1,11):
#    data = "%d ," % x
#    f.write(data)
f.write("12") #txt 파일에 문자열 적기
f.write("34")
f.close() #txt 파일 닫기
#파일을 열고 닫을 때 까지 쓴 문자열만 저장 되면서 이전에 있던 내용을 다 날아감
